<!-- <!DOCTYPE html>
<html>
  <head>
    <title>Chatbot</title>
  </head>
  <body>
    <h1>Chatbot</h1>
    <div id="message-list">
        <p><%= messages %></p>
    </div>
    <form id="chat-form">
      <input type="text" id="user-input">
      <button type="submit">Send</button>
    </form>

    <script>
      const messageList = document.getElementById("message-list");
      const chatForm = document.getElementById("chat-form");
      const userInput = document.getElementById("user-input");
      let chatbotSession = null;

      // Display welcome message and options on page load
      fetch("/chatbot", {
        method: "GET",
      })
        .then(response => response.text())
        .then(data => {
          messageList.innerHTML += `<li>Bot: ${data}</li>`;
          chatbotSession = JSON.parse(data.split("session:")[1]);
        })
        .catch(error => {console.log(error)});

      chatForm.addEventListener("submit", (event) => {
        event.preventDefault();
        const message = userInput.value;
        userInput.value = "";
        sendMessage(message);
      });

      function sendMessage(message) {
        // Append user message to the chat window
        messageList.innerHTML += `<li>You: ${message}</li>`;

        // Send the message to the server
        fetch("/chatbot/place-order", {
          method: "POST",
          headers: {
            "Content-Type": "application/json",
          },
          body: JSON.stringify({
            message: message,
            session: chatbotSession,
          }),
        })
          .then(response => response.json())
          .then(data => {
            // Append chatbot message to the chat window
            messageList.innerHTML += `<li>Bot: ${data.message}</li>`;
            // Update the chatbot session ID
            chatbotSession = data.session;
          })
          .catch(error => {console.log(error)});
      }
    </script>
  </body>
</html> -->